name: Pull Request Target

on:
  pull_request_target:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
      
      - name: Where am I?
        run: ls
        
      - name: Another step
        run: echo This is another step that only exists on my branch
